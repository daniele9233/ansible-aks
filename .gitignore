# Log Ansible
ansible.log
*.log
*.retry

# Kubeconfig — MAI committare
*.kubeconfig
kubeconfig*
.kube/
files/kubeconfig-*.yaml

# Vault decifrati — MAI committare. Vault cifrato OK.
*.vault.dec
group_vars/**/vault.yml.dec
# Protezione extra: blocca vault_cavalid.yml se NON inizia con $ANSIBLE_VAULT
# (il git hook pre-commit nel HOW-TO verifica la riga 1 prima del push)

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Python venv locali
.venv/
venv/
__pycache__/
*.pyc
